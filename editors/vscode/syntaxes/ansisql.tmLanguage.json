{
  "$schema": "https://raw.githubusercontent.com/martinring/tmlanguage/master/tmlanguage.json",
  "name": "ansisql",
  "scopeName": "source.ansisql",
  "patterns": [
    {
      "comment": "Delegates to the SQL grammar VS Code ships. The dialects differ by a few dozen words, and a tokenizer reads strings, comments, numbers and identifiers rather than words — so three near-identical grammars would be three things to maintain and one thing to look at. What is dialect-correct is completion, and that comes from the kernel, which knows which cell asked.",
      "include": "source.sql"
    }
  ]
}
