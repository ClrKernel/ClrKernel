// Monaco's json language worker, as a local worker entry point — see the note in
// editor.worker.ts for why this file exists rather than a `?worker` import
// pointed straight at the package.
import 'monaco-editor/language/json/json.worker.js';
